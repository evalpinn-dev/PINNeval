# Runtime requirements for the anonymous PINNEval review artifact.
#
# JAX installation is hardware-specific. The plain jax/jaxlib requirements below
# are suitable for CPU-oriented smoke tests. For CUDA/TPU environments, install
# the matching JAX build recommended for that platform before running full
# benchmark sweeps.

absl-py
flax
jax
jaxlib
matplotlib
ml-collections
numpy
optax
scipy
soap-jax @ git+https://github.com/haydn-jones/SOAP_JAX@v0.1.0
tabulate
wandb
